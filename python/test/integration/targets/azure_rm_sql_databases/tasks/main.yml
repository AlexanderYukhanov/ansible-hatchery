- name: Create instance of Database
  azure_rm_sql_databases:
    resource_group: "{{ resource_group }}"
    server_name: zims-server
    name: test-database
    location: westus
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.status == 'Online'
