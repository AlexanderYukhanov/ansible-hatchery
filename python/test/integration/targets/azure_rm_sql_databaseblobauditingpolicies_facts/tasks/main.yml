- name: Create instance of DatabaseBlobAuditingPolicies
  azure_rm_sql_databaseblobauditingpolicies_facts:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
