- name: Create instance of ServerCommunicationLinks
  azure_rm_sql_servercommunicationlinks_facts:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state.state == 'Ready'
