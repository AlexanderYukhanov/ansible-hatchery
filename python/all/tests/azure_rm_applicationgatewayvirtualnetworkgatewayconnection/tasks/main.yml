- name: Prepare random number
  set_fact:
    random_postfix: "{{ 1000 | random }}"
  run_once: yes

- name: Create instance of VirtualNetworkGatewayConnections -- check mode
  azure_rm_applicationgatewayvirtualnetworkgatewayconnection:
    resource_group: "{{ resource_group }}"
    virtual_network_gateway1:
      ip_configurations:
      vpn_client_configuration:
        vpn_client_address_pool:
          address_prefixes:
            - XXXX - list of values -- not implemented str
        vpn_client_root_certificates:
        vpn_client_revoked_certificates:
        vpn_client_protocols:
          - XXXX - list of values -- not implemented str
    virtual_network_gateway2:
      ip_configurations:
      vpn_client_configuration:
        vpn_client_address_pool:
          address_prefixes:
            - XXXX - list of values -- not implemented str
        vpn_client_root_certificates:
        vpn_client_revoked_certificates:
        vpn_client_protocols:
          - XXXX - list of values -- not implemented str
    local_network_gateway2:
      local_network_address_space:
        address_prefixes:
          - XXXX - list of values -- not implemented str
    ipsec_policies:
  check_mode: yes
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed

- name: Create instance of VirtualNetworkGatewayConnections
  azure_rm_applicationgatewayvirtualnetworkgatewayconnection:
    resource_group: "{{ resource_group }}"
    virtual_network_gateway1:
      ip_configurations:
      vpn_client_configuration:
        vpn_client_address_pool:
          address_prefixes:
            - XXXX - list of values -- not implemented str
        vpn_client_root_certificates:
        vpn_client_revoked_certificates:
        vpn_client_protocols:
          - XXXX - list of values -- not implemented str
    virtual_network_gateway2:
      ip_configurations:
      vpn_client_configuration:
        vpn_client_address_pool:
          address_prefixes:
            - XXXX - list of values -- not implemented str
        vpn_client_root_certificates:
        vpn_client_revoked_certificates:
        vpn_client_protocols:
          - XXXX - list of values -- not implemented str
    local_network_gateway2:
      local_network_address_space:
        address_prefixes:
          - XXXX - list of values -- not implemented str
    ipsec_policies:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state == 'Ready'

- name: Create again instance of VirtualNetworkGatewayConnections
  azure_rm_applicationgatewayvirtualnetworkgatewayconnection:
    resource_group: "{{ resource_group }}"
    virtual_network_gateway1:
      ip_configurations:
      vpn_client_configuration:
        vpn_client_address_pool:
          address_prefixes:
            - XXXX - list of values -- not implemented str
        vpn_client_root_certificates:
        vpn_client_revoked_certificates:
        vpn_client_protocols:
          - XXXX - list of values -- not implemented str
    virtual_network_gateway2:
      ip_configurations:
      vpn_client_configuration:
        vpn_client_address_pool:
          address_prefixes:
            - XXXX - list of values -- not implemented str
        vpn_client_root_certificates:
        vpn_client_revoked_certificates:
        vpn_client_protocols:
          - XXXX - list of values -- not implemented str
    local_network_gateway2:
      local_network_address_space:
        address_prefixes:
          - XXXX - list of values -- not implemented str
    ipsec_policies:
  register: output
- name: Assert the state has not changed
  assert:
    that:
      - output.changed == false
      - output.state == 'Ready'

- name: Delete instance of VirtualNetworkGatewayConnections -- check mode
  azure_rm_applicationgatewayvirtualnetworkgatewayconnection:
    resource_group: "{{ resource_group }}"
    state: absent
  check_mode: yes
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete instance of VirtualNetworkGatewayConnections
  azure_rm_applicationgatewayvirtualnetworkgatewayconnection:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete unexisting instance of VirtualNetworkGatewayConnections
  azure_rm_applicationgatewayvirtualnetworkgatewayconnection:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed == false
