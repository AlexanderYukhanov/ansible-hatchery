- name: Prepare random number
  set_fact:
    random_postfix: "{{ 1000 | random }}"
  run_once: yes

- name: Create instance of LocalNetworkGateways -- check mode
  azure_rm_applicationgatewaylocalnetworkgateway:
    resource_group: "{{ resource_group }}"
    local_network_address_space:
      address_prefixes:
        - XXXX - list of values -- not implemented str
  check_mode: yes
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed

- name: Create instance of LocalNetworkGateways
  azure_rm_applicationgatewaylocalnetworkgateway:
    resource_group: "{{ resource_group }}"
    local_network_address_space:
      address_prefixes:
        - XXXX - list of values -- not implemented str
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state == 'Ready'

- name: Create again instance of LocalNetworkGateways
  azure_rm_applicationgatewaylocalnetworkgateway:
    resource_group: "{{ resource_group }}"
    local_network_address_space:
      address_prefixes:
        - XXXX - list of values -- not implemented str
  register: output
- name: Assert the state has not changed
  assert:
    that:
      - output.changed == false
      - output.state == 'Ready'

- name: Delete instance of LocalNetworkGateways -- check mode
  azure_rm_applicationgatewaylocalnetworkgateway:
    resource_group: "{{ resource_group }}"
    state: absent
  check_mode: yes
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete instance of LocalNetworkGateways
  azure_rm_applicationgatewaylocalnetworkgateway:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete unexisting instance of LocalNetworkGateways
  azure_rm_applicationgatewaylocalnetworkgateway:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed == false
