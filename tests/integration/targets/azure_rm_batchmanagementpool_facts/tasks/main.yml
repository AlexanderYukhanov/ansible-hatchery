- name: Prepare random number
  set_fact:
    rpfx: "{{ resource_group | hash('md5') | truncate(7, True, '') }}{{ 1000 | random }}"
  run_once: yes

- name: Gather facts Pool
  azure_rm_batchmanagementpool_facts:
    resource_group: "{{ resource_group }}"
  register: output
- name: Assert that facts are returned
  assert:
    that:
      - output.changed == False
      - output.pool.xxxunknownxxx.id != None
      - output.pool.xxxunknownxxx.name != None
      - output.pool.xxxunknownxxx.type != None
      - output.pool.xxxunknownxxx.etag != None
      - output.pool.xxxunknownxxxsecond.id != None
      - output.pool.xxxunknownxxxsecond.name != None
      - output.pool.xxxunknownxxxsecond.type != None
      - output.pool.xxxunknownxxxsecond.etag != None

- name: Gather facts Pool
  azure_rm_batchmanagementpool_facts:
    resource_group: "{{ resource_group }}"
  register: output
- name: Assert that facts are returned
  assert:
    that:
      - output.changed == False
      - output.pool.xxxunknownxxx.id != None
      - output.pool.xxxunknownxxx.name != None
      - output.pool.xxxunknownxxx.type != None
      - output.pool.xxxunknownxxx.etag != None
