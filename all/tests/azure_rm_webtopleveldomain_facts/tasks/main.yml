- name: Prepare random number
  set_fact:
    random_postfix: "{{ 1000 | random }}"
  run_once: yes

- name: Gather facts TopLevelDomains
  azure_rm_webtopleveldomain_facts:
  register: output
- name: Assert that facts are returned
  assert:
    that:
      - output.changed == False

- name: Gather facts TopLevelDomains
  azure_rm_webtopleveldomain_facts:
  register: output
- name: Assert that facts are returned
  assert:
    that:
      - output.changed == False
