- name: Prepare random number
  set_fact:
    random_postfix: "{{ 1000 | random }}"
  run_once: yes

- name: Create instance of SecurityRules -- check mode
  azure_rm_applicationgatewaysecurityrule:
    resource_group: "{{ resource_group }}"
    source_address_prefixes:
      - XXXX - list of values -- not implemented str
    source_application_security_groups:
    destination_address_prefixes:
      - XXXX - list of values -- not implemented str
    destination_application_security_groups:
    source_port_ranges:
      - XXXX - list of values -- not implemented str
    destination_port_ranges:
      - XXXX - list of values -- not implemented str
  check_mode: yes
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed

- name: Create instance of SecurityRules
  azure_rm_applicationgatewaysecurityrule:
    resource_group: "{{ resource_group }}"
    source_address_prefixes:
      - XXXX - list of values -- not implemented str
    source_application_security_groups:
    destination_address_prefixes:
      - XXXX - list of values -- not implemented str
    destination_application_security_groups:
    source_port_ranges:
      - XXXX - list of values -- not implemented str
    destination_port_ranges:
      - XXXX - list of values -- not implemented str
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed

- name: Create again instance of SecurityRules
  azure_rm_applicationgatewaysecurityrule:
    resource_group: "{{ resource_group }}"
    source_address_prefixes:
      - XXXX - list of values -- not implemented str
    source_application_security_groups:
    destination_address_prefixes:
      - XXXX - list of values -- not implemented str
    destination_application_security_groups:
    source_port_ranges:
      - XXXX - list of values -- not implemented str
    destination_port_ranges:
      - XXXX - list of values -- not implemented str
  register: output
- name: Assert the state has not changed
  assert:
    that:
      - output.changed == false

- name: Delete instance of SecurityRules -- check mode
  azure_rm_applicationgatewaysecurityrule:
    resource_group: "{{ resource_group }}"
    state: absent
  check_mode: yes
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete instance of SecurityRules
  azure_rm_applicationgatewaysecurityrule:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete unexisting instance of SecurityRules
  azure_rm_applicationgatewaysecurityrule:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed == false
