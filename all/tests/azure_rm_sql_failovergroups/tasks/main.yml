- name: Create instance of FailoverGroups
  azure_rm_sql_failovergroups:
    resource_group: "{{ resource_group }}"
    partner_servers:
    databases:
      - XXXX - list of values -- not implemented str
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state == 'Ready'

- name: Update instance of FailoverGroups
  azure_rm_sql_failovergroups:
    resource_group: "{{ resource_group }}"
    partner_servers:
    databases:
      - XXXX - list of values -- not implemented str
  register: output
- name: Assert the state has not changed
  assert:
    that:
      - output.changed == false
      - output.state == 'Ready'

- name: Delete instance of FailoverGroups
  azure_rm_sql_failovergroups:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed
