- name: Prepare random number
  set_fact:
    random_postfix: "{{ 1000 | random }}"
  run_once: yes

- name: Gather facts ApplicationGateways
  azure_rm_applicationgatewayapplicationgateway_facts:
    resource_group: "{{ resource_group }}"
    application_gateway_name: mymanualgateway
  register: output
- name: Assert that facts are returned
  assert:
    that:
      - output.changed == False

- name: Gather facts ApplicationGateways
  azure_rm_applicationgatewayapplicationgateway_facts:
  register: output
- name: Assert that facts are returned
  assert:
    that:
      - output.changed == False

- name: Gather facts ApplicationGateways
  azure_rm_applicationgatewayapplicationgateway_facts:
  register: output
- name: Assert that facts are returned
  assert:
    that:
      - output.changed == False

- name: Gather facts ApplicationGateways
  azure_rm_applicationgatewayapplicationgateway_facts:
  register: output
- name: Assert that facts are returned
  assert:
    that:
      - output.changed == False

- name: Gather facts ApplicationGateways
  azure_rm_applicationgatewayapplicationgateway_facts:
  register: output
- name: Assert that facts are returned
  assert:
    that:
      - output.changed == False
