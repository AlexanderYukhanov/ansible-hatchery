- name: Prepare random number
  set_fact:
    random_postfix: "{{ 1000 | random }}"
  run_once: yes

- name: Create instance of FailoverGroups -- check mode
  azure_rm_sqlfailovergroup:
    resource_group: "{{ resource_group }}"
    partner_servers:
    databases:
      - XXXX - list of values -- not implemented str
  check_mode: yes
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed

- name: Create instance of FailoverGroups
  azure_rm_sqlfailovergroup:
    resource_group: "{{ resource_group }}"
    partner_servers:
    databases:
      - XXXX - list of values -- not implemented str
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state == 'Ready'

- name: Create again instance of FailoverGroups -- check mode
  azure_rm_sqlfailovergroup:
    resource_group: "{{ resource_group }}"
    partner_servers:
    databases:
      - XXXX - list of values -- not implemented str
  register: output
- name: Assert the state has not changed
  assert:
    that:
      - output.changed == false

- name: Create again instance of FailoverGroups
  azure_rm_sqlfailovergroup:
    resource_group: "{{ resource_group }}"
    partner_servers:
    databases:
      - XXXX - list of values -- not implemented str
  register: output
- name: Assert the state has not changed
  assert:
    that:
      - output.changed == false
      - output.state == 'Ready'

- name: Delete instance of FailoverGroups -- check mode
  azure_rm_sqlfailovergroup:
    resource_group: "{{ resource_group }}"
    state: absent
  check_mode: yes
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete instance of FailoverGroups
  azure_rm_sqlfailovergroup:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete unexisting instance of FailoverGroups
  azure_rm_sqlfailovergroup:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed == false
