- name: Prepare random number
  set_fact:
    random_postfix: "{{ 1000 | random }}"
  run_once: yes

- name: Create instance of WebApps -- check mode
  azure_rm_webwebapp:
    resource_group: "{{ resource_group }}"
    host_name_ssl_states:
    site_config:
      default_documents:
        - XXXX - list of values -- not implemented str
      app_settings:
      connection_strings:
      handler_mappings:
      virtual_applications:
        - virtual_directories:
      experiments:
        ramp_up_rules:
      auto_heal_rules:
        triggers:
          status_codes:
      cors:
        allowed_origins:
          - XXXX - list of values -- not implemented str
      ip_security_restrictions:
  check_mode: yes
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed

- name: Create instance of WebApps
  azure_rm_webwebapp:
    resource_group: "{{ resource_group }}"
    host_name_ssl_states:
    site_config:
      default_documents:
        - XXXX - list of values -- not implemented str
      app_settings:
      connection_strings:
      handler_mappings:
      virtual_applications:
        - virtual_directories:
      experiments:
        ramp_up_rules:
      auto_heal_rules:
        triggers:
          status_codes:
      cors:
        allowed_origins:
          - XXXX - list of values -- not implemented str
      ip_security_restrictions:
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed
      - output.state == 'Ready'

- name: Create again instance of WebApps
  azure_rm_webwebapp:
    resource_group: "{{ resource_group }}"
    host_name_ssl_states:
    site_config:
      default_documents:
        - XXXX - list of values -- not implemented str
      app_settings:
      connection_strings:
      handler_mappings:
      virtual_applications:
        - virtual_directories:
      experiments:
        ramp_up_rules:
      auto_heal_rules:
        triggers:
          status_codes:
      cors:
        allowed_origins:
          - XXXX - list of values -- not implemented str
      ip_security_restrictions:
  register: output
- name: Assert the state has not changed
  assert:
    that:
      - output.changed == false
      - output.state == 'Ready'

- name: Delete instance of WebApps -- check mode
  azure_rm_webwebapp:
    resource_group: "{{ resource_group }}"
    state: absent
  check_mode: yes
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete instance of WebApps
  azure_rm_webwebapp:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete unexisting instance of WebApps
  azure_rm_webwebapp:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed == false
