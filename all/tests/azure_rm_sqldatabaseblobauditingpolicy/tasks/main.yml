- name: Prepare random number
  set_fact:
    random_postfix: "{{ 1000 | random }}"
  run_once: yes

- name: Create instance of DatabaseBlobAuditingPolicies -- check mode
  azure_rm_sqldatabaseblobauditingpolicy:
    resource_group: "{{ resource_group }}"
    audit_actions_and_groups:
      - XXXX - list of values -- not implemented str
  check_mode: yes
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed

- name: Create instance of DatabaseBlobAuditingPolicies
  azure_rm_sqldatabaseblobauditingpolicy:
    resource_group: "{{ resource_group }}"
    audit_actions_and_groups:
      - XXXX - list of values -- not implemented str
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed

- name: Create again instance of DatabaseBlobAuditingPolicies
  azure_rm_sqldatabaseblobauditingpolicy:
    resource_group: "{{ resource_group }}"
    audit_actions_and_groups:
      - XXXX - list of values -- not implemented str
  register: output
- name: Assert the state has not changed
  assert:
    that:
      - output.changed == false
