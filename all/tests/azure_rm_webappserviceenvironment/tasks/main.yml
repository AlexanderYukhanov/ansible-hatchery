- name: Prepare random number
  set_fact:
    random_postfix: "{{ 1000 | random }}"
  run_once: yes

- name: Create instance of AppServiceEnvironments -- check mode
  azure_rm_webappserviceenvironment:
    resource_group: "{{ resource_group }}"
    worker_pools:
    network_access_control_list:
    cluster_settings:
    user_whitelisted_ip_ranges:
      - XXXX - list of values -- not implemented str
  check_mode: yes
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed

- name: Create instance of AppServiceEnvironments
  azure_rm_webappserviceenvironment:
    resource_group: "{{ resource_group }}"
    worker_pools:
    network_access_control_list:
    cluster_settings:
    user_whitelisted_ip_ranges:
      - XXXX - list of values -- not implemented str
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed

- name: Create again instance of AppServiceEnvironments
  azure_rm_webappserviceenvironment:
    resource_group: "{{ resource_group }}"
    worker_pools:
    network_access_control_list:
    cluster_settings:
    user_whitelisted_ip_ranges:
      - XXXX - list of values -- not implemented str
  register: output
- name: Assert the state has not changed
  assert:
    that:
      - output.changed == false

- name: Delete instance of AppServiceEnvironments -- check mode
  azure_rm_webappserviceenvironment:
    resource_group: "{{ resource_group }}"
    state: absent
  check_mode: yes
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete instance of AppServiceEnvironments
  azure_rm_webappserviceenvironment:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete unexisting instance of AppServiceEnvironments
  azure_rm_webappserviceenvironment:
    resource_group: "{{ resource_group }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed == false
