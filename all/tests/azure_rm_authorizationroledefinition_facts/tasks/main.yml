- name: Prepare random number
  set_fact:
    random_postfix: "{{ 1000 | random }}"
  run_once: yes

- name: Gather facts RoleDefinitions
  azure_rm_authorizationroledefinition_facts:
  register: output
- name: Assert that facts are returned
  assert:
    that:
      - output.changed == False
